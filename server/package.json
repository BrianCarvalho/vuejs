{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js",
    "nodemon":"nodemon app.js --exec'npm run lint && node'",
    "lint": "eslint *.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3",
    "devDependencies": {
      "eslint": "^5.6.1",
      "nodemon": "^1.18.4"
    }
  }
}
